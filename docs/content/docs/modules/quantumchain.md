---
title: "quantumchain"
weight: 3
---

The `x/quantumchain` module is the chain's base module — it owns chain-wide parameters and identity. Add a detailed reference here once the parameter set is finalised.
